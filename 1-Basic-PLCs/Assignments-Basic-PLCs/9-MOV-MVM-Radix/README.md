# MOVE, MASKED MOVE AND RADIX

- MOV is similar to a COP instruction
- MVM uses a "mask" to filter out certain bits from Source to Destination
- RADIX is the style that information is displayed:
    - Decimal format
    - Binary format
    - Octal format
    - Hex format
    - ASCII format
