# GSV - GET SYSTEM VALUE

- Requires an Array of DINT[7] to store WallClockTime data
- Requires a ONS to prevent storing data every scan cycle
- Date/Time values may not match current UTC
    - _There is a way to set the controller time to match the workstation, but it's finicky_
